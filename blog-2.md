_How do Pick and Omit utility types prevent code duplication while creating specialized "slices" of a master interface? Discuss how this keeps your code DRY (Don't Repeat Yourself)._

# `Pick` and `Omit`

`Pick` and `Omit` are utilities type in `Typescript`
